<?php
/**
 * if you wish to alter this configuration file, make sure you issue the publish command and change the published version:
 * # php artisan config:publish netson/l4shell
 */
return array(
    /**
     * enable or disable logging; all aspects of the shell command will be logged (command, success/fail, arguments)
     * the default is [en_US]
     */
    'enable_logging' => true,
);

?>